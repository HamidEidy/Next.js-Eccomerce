'use client'

//import { deleteUserAddress } from "@/utils/fetchWithToken"

const DeleteAddress = ({ id }: any) => {
    // const DeleteAddres = async () => {
    //     const data = await deleteUserAddress('/profile/address/delete', { id })
    // }

    return (
        // <button className="btn btn-dark" >حذف</button>
    )
}
export default DeleteAddress;